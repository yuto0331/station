<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :restaurant_image %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :restaurant_name %>
  </div>

  <div class="field">
    <%= form.label :place %>
    <%= form.text_field :place, id: :restaurant_place %>
  </div>

  <!--<div class="field">-->
  <!--  <%#= form.label :user_id %>-->
  <!--  <%#= form.number_field :user_id, id: :restaurant_user_id %>-->
  <!--</div>-->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
